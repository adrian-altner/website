<footer>
	<div class="footer-inner">
		<a href="/ueber-mich" aria-label="Über mich">
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M20 21a8 8 0 0 0-16 0"></path>
				<circle cx="12" cy="7" r="4"></circle>
			</svg>
		</a>
		<a href="/rss.xml" aria-label="Abonnieren">
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M4 11a9 9 0 0 1 9 9"></path>
				<path d="M4 4a16 16 0 0 1 16 16"></path>
				<circle cx="5" cy="19" r="1"></circle>
			</svg>
		</a>
		<a href="/kolophon" aria-label="Kolophon">
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<circle cx="12" cy="12" r="10"></circle>
				<line x1="12" y1="16" x2="12" y2="12"></line>
				<line x1="12" y1="8" x2="12.01" y2="8"></line>
			</svg>
		</a>
		<a href="/impressum" aria-label="Impressum">
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"></path>
				<path d="M14 2v4a2 2 0 0 0 2 2h4"></path>
				<path d="M10 9H8"></path>
				<path d="M16 13H8"></path>
				<path d="M16 17H8"></path>
			</svg>
		</a>
		<a href="/datenschutz" aria-label="Datenschutz">
			<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M20 13c0 5-3.5 7.5-7.5 8.5-.4.1-.6.1-1 0C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"></path>
			</svg>
		</a>
	</div>
</footer>

<style>
	footer {
		width: var(--content-width);
		max-width: 100%;
		margin: 0 auto;
		padding: 0 1em 2em;
	}
	.footer-inner {
		display: flex;
		gap: 1em;
		align-items: center;
		justify-content: flex-end;
	}
	.footer-inner a {
		position: relative;
		display: inline-flex;
		color: var(--muted);
	}
	.footer-inner a:hover {
		color: var(--accent);
	}
	.footer-inner svg {
		width: 1.3em;
		height: 1.3em;
	}
	.footer-inner a::after {
		content: attr(aria-label);
		position: absolute;
		bottom: 100%;
		left: 50%;
		transform: translateX(-50%) translateY(-0.4em);
		background: var(--text);
		color: var(--bg);
		font-family: var(--font-heading), system-ui, sans-serif;
		font-size: 0.75rem;
		white-space: nowrap;
		padding: 0.3em 0.6em;
		border-radius: 4px;
		opacity: 0;
		pointer-events: none;
		transition:
			opacity 0.15s ease,
			transform 0.15s ease;
	}
	.footer-inner a:hover::after {
		opacity: 1;
		transform: translateX(-50%) translateY(-0.6em);
	}
	/* The group is right-aligned (flex-end), so a centered tooltip on the
	   last icon can overflow past the viewport's right edge on narrow
	   screens — anchor it to the icon's right edge instead of its center. */
	.footer-inner a:last-child::after {
		left: auto;
		right: 0;
		transform: translateY(-0.4em);
	}
	.footer-inner a:last-child:hover::after {
		transform: translateY(-0.6em);
	}
</style>
