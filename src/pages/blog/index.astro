---
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import BaseLayout from '@/layouts/BaseLayout.astro';
import FormattedDate from '@/components/FormattedDate.astro';

type BlogEntry = CollectionEntry<'blog'>;

const title = 'Blog';
const description = 'Latest writing, learnings, and announcements.';

const includeDrafts = import.meta.env.DEV;

const posts: BlogEntry[] = (await getCollection('blog'))
	.filter((post) => includeDrafts || post.data.draft !== true)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout title={title} description={description}>
	<section class="posts">
		<table class="post-table">
			<thead>
				<tr>
					<th scope="col">Title</th>
					<th scope="col">Published</th>
				</tr>
			</thead>
			<tbody>
				{
					posts.map((post) => {
						const { title: postTitle, pubDate, draft } = post.data;
						const href = `/blog/${post.id}/`;

						return (
							<tr class={draft ? 'post-table__row--draft' : undefined}>
								<td class="post-table__title">
									<a href={href}>
										<span>
											{postTitle}
											{draft ? (
												<strong class="badge badge--draft">
													Draft
												</strong>
											) : null}
										</span>
									</a>
								</td>
								<td class="post-table__date">
										<FormattedDate date={pubDate} />
								</td>
							</tr>
						);
					})
				}
			</tbody>
		</table>
	</section>
</BaseLayout>

<style>
	.posts {
		width: 720px;
		max-width: calc(100% - 2rem);
		margin: 0 auto;
		padding: 2rem 0 4rem;
	}

	.post-table {
		width: 100%;
		border-collapse: collapse;
		background: white;
		box-shadow: var(--box-shadow);
		border-radius: 12px;
		overflow: hidden;
	}

	.post-table th,
	.post-table td {
		padding: 1rem;
		text-align: left;
		border-bottom: 1px solid rgba(var(--black), 0.08);
		vertical-align: middle;
	}

	.post-table thead {
		background: rgb(var(--gray-light), 0.80);
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-size: 0.85rem;
		font-weight: 600;
		color: rgb(var(--gray));
	}

	.post-table tr:last-child td {
		border-bottom: none;
	}

	.post-table a {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		color: inherit;
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.post-table__title a {
		font-weight: 600;
		color: rgb(var(--black));
	}

	.post-table__date {
		font-size: .8em;
		white-space: nowrap;
		width: 8rem;
	}

	.post-table a:hover {
		color: var(--accent);
	}

	.post-table tbody tr:hover {
		background: rgba(var(--gray), 0.08);
	}

	.post-table__title a:hover {
		color: var(--accent);
	}

	.badge {
		display: inline-flex;
		align-items: center;
		margin-left: 0.5rem;
		font-size: 0.65rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		border-radius: 999px;
		padding: 0.1rem 0.45rem;
		font-weight: 700;
	}

	.badge--draft {
		color: rgb(var(--accent));
		background: rgba(var(--accent), 0.12);
	}

	.post-table__row--draft {
		opacity: 0.8;
	}

	@media (max-width: 720px) {
		.post-table,
		.post-table thead,
		.post-table tbody,
		.post-table th,
		.post-table td,
		.post-table tr {
			display: block;
			width: 100%;
		}

		.post-table thead {
			position: absolute;
			clip: rect(0 0 0 0);
			height: 1px;
			width: 1px;
			overflow: hidden;
		}

		.post-table tr {
			border-bottom: 1px solid rgba(var(--black), 0.08);
		}

		.post-table td {
			border: none;
			padding: 1rem 1rem 0;
		}

		.post-table td:last-child {
			padding-bottom: 1rem;
		}

		.post-table__title span {
			font-size: 1.125rem;
		}

		.post-table__date {
			padding-top: 0.25rem;
			color: rgb(var(--gray));
		}
	}
</style>
